# from apis.wallets.views import PaymentCardModelViewset

from rest_framework.routers import SimpleRouter

router = SimpleRouter()
# router.register('payment-card', PaymentCardModelViewset, basename='orders')


urlpatterns = [
    # *router.urls
]
